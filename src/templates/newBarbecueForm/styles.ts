// Packages
import styled from 'styled-components';

export const newBarbecueContainer = styled.section`
  background-color: ${({ theme }) => theme.colors.white};
  border-radius: 8px;
  padding: 2rem;
  margin-top: -20px;
`;
